{
  "$comment": "Auto-generated — do not edit directly. Run `pnpm generate:config-schema` to regenerate this file from packages/config-schema/src/valeros-config.schema.ts.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://valeros.nl/config/valeros.config.schema.json",
  "type": "object",
  "properties": {
    "$schema": {
      "title": "JSON Schema",
      "readOnly": true,
      "type": "string"
    },
    "api": {
      "type": "object",
      "properties": {
        "baseUrl": {
          "type": "string",
          "title": "Data layer URL",
          "examples": [
            "http://localhost:4000/graphql"
          ]
        }
      },
      "required": [
        "baseUrl"
      ],
      "additionalProperties": false,
      "title": "Data layer",
      "description": "Connection to the data layer."
    },
    "facets": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "title": "Name",
            "description": "Facet property name in the data layer.",
            "examples": [
              "creator"
            ]
          },
          "label": {
            "type": "string",
            "title": "Label",
            "description": "Name shown to visitors for this filter.",
            "examples": [
              "Creator"
            ]
          },
          "icon": {
            "$ref": "#/$defs/iconKey"
          },
          "hidden": {
            "title": "Hidden",
            "description": "Hide this filter from visitors.",
            "type": "boolean"
          }
        },
        "required": [
          "name",
          "label"
        ],
        "additionalProperties": false,
        "title": "Filter",
        "description": "Filter visitors can use to refine the results."
      },
      "title": "Filters",
      "description": "Filters visitors can use to refine the results."
    },
    "views": {
      "type": "object",
      "properties": {
        "views": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "type": {
                "$ref": "#/$defs/viewType"
              },
              "componentId": {
                "$ref": "#/$defs/viewComponentKey"
              },
              "icon": {
                "$ref": "#/$defs/iconKey"
              },
              "label": {
                "type": "string",
                "title": "Label",
                "description": "Accessible label for the view selector icon.",
                "examples": [
                  "List view"
                ]
              },
              "options": {
                "type": "object",
                "properties": {
                  "pageSize": {
                    "title": "Results per page",
                    "description": "Number of results per page for this view.",
                    "type": "number"
                  },
                  "showPagination": {
                    "title": "Show pagination controls",
                    "type": "boolean"
                  },
                  "showResultsCount": {
                    "title": "Show total number of search results",
                    "type": "boolean"
                  },
                  "showSort": {
                    "title": "Show sorting controls",
                    "type": "boolean"
                  },
                  "hidden": {
                    "title": "Hide from the view selector",
                    "type": "boolean"
                  },
                  "defaultSort": {
                    "title": "Default sorting",
                    "description": "Sorting applied when this view opens.",
                    "examples": [
                      "dateCreated:asc"
                    ],
                    "type": "string"
                  }
                },
                "additionalProperties": {}
              },
              "presentationConfig": {
                "title": "Search result presentation",
                "description": "Presentation of search results in this view.",
                "$ref": "#/$defs/nodePresentationConfig"
              }
            },
            "required": [
              "type",
              "componentId",
              "icon",
              "label",
              "options",
              "presentationConfig"
            ],
            "additionalProperties": false,
            "title": "View",
            "description": "A way to display search results."
          },
          "title": "Views",
          "description": "Available views for search results."
        },
        "defaultView": {
          "title": "Default view",
          "description": "View that opens first.",
          "$ref": "#/$defs/viewType"
        }
      },
      "required": [
        "views",
        "defaultView"
      ],
      "additionalProperties": false,
      "title": "Search views",
      "description": "Settings for displaying search results. Each view visualizes the search results in a different way."
    },
    "detailsPresentation": {
      "type": "object",
      "properties": {
        "widgets": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "title": "ID",
                "description": "Unique identifier of the widget.",
                "examples": [
                  "creator"
                ]
              },
              "componentId": {
                "$ref": "#/$defs/widgetComponentKey"
              },
              "properties": {
                "title": "Properties",
                "description": "Data layer properties that cause this widget to render. Example: show an image-gallery-widget for the associatedMedia property. If no properties are listed, the widget is always shown.",
                "type": "array",
                "items": {
                  "type": "string",
                  "title": "Property",
                  "examples": [
                    "creator"
                  ]
                }
              },
              "options": {
                "type": "object",
                "properties": {
                  "showPropertyLabel": {
                    "title": "Show property label",
                    "description": "Show a property label for the value.",
                    "type": "boolean"
                  },
                  "propertyLabel": {
                    "title": "Property label",
                    "description": "Custom property label shown for the value.",
                    "examples": [
                      "Creator"
                    ],
                    "type": "string"
                  },
                  "propertyPath": {
                    "title": "Property path",
                    "description": "Path to a nested value within the property.",
                    "examples": [
                      "associatedMedia.thumbnailUrl"
                    ],
                    "type": "string"
                  },
                  "icon": {
                    "$ref": "#/$defs/iconKey"
                  },
                  "position": {
                    "$ref": "#/$defs/widgetPosition"
                  },
                  "noPadding": {
                    "title": "Remove padding",
                    "description": "Extend the widget to the surrounding edges.",
                    "type": "boolean"
                  },
                  "showOriginalLink": {
                    "title": "Show original link",
                    "description": "Show a link to the original value alongside the widget. Example: for a map widget, show a link to the location's details page.",
                    "type": "boolean"
                  }
                },
                "additionalProperties": {}
              },
              "isFallback": {
                "title": "Fallback widget",
                "description": "Use this widget for data layer properties that are not handled by other widgets.",
                "type": "boolean"
              },
              "hidden": {
                "title": "Hidden",
                "description": "Hide this widget from visitors, even when its configured properties are present in the data layer.",
                "type": "boolean"
              }
            },
            "required": [
              "id",
              "componentId"
            ],
            "additionalProperties": false,
            "title": "Widget",
            "description": "Component that displays one or more data properties."
          },
          "title": "Widgets",
          "description": "Widgets are displayed in this order. A widget is shown for each of its configured data layer properties. For example, a map widget might be used to visualize data for the location, contentLocation, and locationCreated properties."
        }
      },
      "required": [
        "widgets"
      ],
      "additionalProperties": false,
      "title": "Details page",
      "description": "Presentation of an object on the details page."
    },
    "imagePaths": {
      "type": "array",
      "items": {
        "type": "string",
        "title": "Property path",
        "examples": [
          "associatedMedia.thumbnailUrl"
        ]
      },
      "title": "Image properties",
      "description": "Property paths searched in order to find an object's image(s). The first match is used."
    }
  },
  "required": [
    "api",
    "facets",
    "views",
    "detailsPresentation",
    "imagePaths"
  ],
  "additionalProperties": false,
  "title": "Valeros configuration",
  "description": "Settings for how Valeros presents information from the data layer to end users.",
  "$defs": {
    "iconKey": {
      "type": "string",
      "enum": [
        "alert-triangle",
        "align-left",
        "archive",
        "briefcase",
        "calendar",
        "external-link",
        "file-text",
        "filter",
        "grid",
        "home",
        "image",
        "info",
        "link",
        "list",
        "map",
        "map-pin",
        "package",
        "tag",
        "user",
        "users",
        "maximize"
      ],
      "title": "Icon",
      "description": "Icon from the built-in icon registry."
    },
    "viewType": {
      "type": "string",
      "enum": [
        "list",
        "grid",
        "map",
        "timeline"
      ],
      "title": "Type",
      "description": "Unique name of the view."
    },
    "viewComponentKey": {
      "type": "string",
      "enum": [
        "list-view",
        "grid-view",
        "map-view",
        "timeline-view"
      ],
      "title": "Component",
      "description": "Internal component that renders this view."
    },
    "widgetComponentKey": {
      "type": "string",
      "enum": [
        "text-widget",
        "link-widget",
        "literal-link-widget",
        "json-widget",
        "image-gallery-widget",
        "map-widget",
        "media-widget",
        "dataset-widget",
        "creator-widget",
        "address-widget",
        "date-widget",
        "referring-nodes-widget",
        "separator-widget"
      ],
      "title": "Component",
      "description": "Internal widget that displays the data."
    },
    "widgetPosition": {
      "type": "string",
      "enum": [
        "top",
        "left",
        "main",
        "right",
        "bottom"
      ],
      "title": "Position",
      "description": "Position of the widget within the presentation."
    },
    "nodePresentationConfig": {
      "type": "object",
      "properties": {
        "widgets": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "title": "ID",
                "description": "Unique identifier of the widget.",
                "examples": [
                  "creator"
                ]
              },
              "componentId": {
                "$ref": "#/$defs/widgetComponentKey"
              },
              "properties": {
                "title": "Properties",
                "description": "Data layer properties that cause this widget to render. Example: show an image-gallery-widget for the associatedMedia property. If no properties are listed, the widget is always shown.",
                "type": "array",
                "items": {
                  "type": "string",
                  "title": "Property",
                  "examples": [
                    "creator"
                  ]
                }
              },
              "options": {
                "type": "object",
                "properties": {
                  "showPropertyLabel": {
                    "title": "Show property label",
                    "description": "Show a property label for the value.",
                    "type": "boolean"
                  },
                  "propertyLabel": {
                    "title": "Property label",
                    "description": "Custom property label shown for the value.",
                    "examples": [
                      "Creator"
                    ],
                    "type": "string"
                  },
                  "propertyPath": {
                    "title": "Property path",
                    "description": "Path to a nested value within the property.",
                    "examples": [
                      "associatedMedia.thumbnailUrl"
                    ],
                    "type": "string"
                  },
                  "icon": {
                    "$ref": "#/$defs/iconKey"
                  },
                  "position": {
                    "$ref": "#/$defs/widgetPosition"
                  },
                  "noPadding": {
                    "title": "Remove padding",
                    "description": "Extend the widget to the surrounding edges.",
                    "type": "boolean"
                  },
                  "showOriginalLink": {
                    "title": "Show original link",
                    "description": "Show a link to the original value alongside the widget. Example: for a map widget, show a link to the location's details page.",
                    "type": "boolean"
                  }
                },
                "additionalProperties": {}
              },
              "isFallback": {
                "title": "Fallback widget",
                "description": "Use this widget for data layer properties that are not handled by other widgets.",
                "type": "boolean"
              },
              "hidden": {
                "title": "Hidden",
                "description": "Hide this widget from visitors, even when its configured properties are present in the data layer.",
                "type": "boolean"
              }
            },
            "required": [
              "id",
              "componentId"
            ],
            "additionalProperties": false,
            "title": "Widget",
            "description": "Component that displays one or more data properties."
          },
          "title": "Widgets",
          "description": "Widgets are displayed in this order. A widget is shown for each of its configured data layer properties. For example, a map widget might be used to visualize data for the location, contentLocation, and locationCreated properties."
        },
        "showArrowIndicator": {
          "title": "Show arrow indicator on clickable objects",
          "type": "boolean"
        }
      },
      "required": [
        "widgets"
      ],
      "additionalProperties": false,
      "title": "Object presentation",
      "description": "Settings for presenting an object."
    }
  }
}
